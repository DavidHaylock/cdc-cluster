apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "cdc-app.api.fullname" . }}
  labels:
    {{- include "cdc-app.api.labels" . | nindent 4 }}
data:
  DATABASE_URL: {{ include "cdc-app.databaseUrl" . | quote }}
  PORT: {{ .Values.api.containerPort | quote }}
