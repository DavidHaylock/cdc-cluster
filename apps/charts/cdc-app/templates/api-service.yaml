apiVersion: v1
kind: Service
metadata:
  name: {{ include "cdc-app.api.fullname" . }}
  labels:
    {{- include "cdc-app.api.labels" . | nindent 4 }}
  {{- with .Values.api.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.api.service.type }}
  ports:
    - name: http
      port: {{ .Values.api.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "cdc-app.api.selectorLabels" . | nindent 4 }}
